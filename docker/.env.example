# Signalforge Dev Environment Configuration

# Project root - path to your project files (will be mounted to /var/www/html)
PROJECT_ROOT=./www

# MySQL Configuration
MYSQL_ROOT_PASSWORD=signalforge
MYSQL_DATABASE=app
MYSQL_USER=dev
MYSQL_PASSWORD=dev

# PostgreSQL Configuration
POSTGRES_DB=app
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
